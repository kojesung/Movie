const logout = () => {

    return (
        <div>
            <button style={{}} onClick={handleLogin}>
                로그아웃
            </button>
            <div style={{ color: "white", marginLeft: "10px" }}>
                환영합니다!
            </div>
        </div>
    );
}